# VPC CNI network policy ile uygulanir (vpc-cni addon'unda
# enableNetworkPolicy=true set edildi).
#
# Amac: hive pod'u ele gecirilirse oradan baska bir yere gidilemesin.
# Izin verilen tek giden trafik: DNS + RDS 5432. Internete cikis kapali.
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: hive
  namespace: hive
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: hive

  policyTypes:
    - Ingress
    - Egress

  ingress:
    # ALB target-type=ip ile pod IP'sine dogrudan gelir; kaynak ALB ENI'si
    # oldugu icin VPC icinden 8080'e izin veriliyor.
    - from:
        - ipBlock:
            cidr: 10.20.0.0/16 # VPC CIDR (deploy/terraform var.vpc_cidr)
      ports:
        - protocol: TCP
          port: 8080
        # metrik toplayici (Prometheus / CloudWatch agent) VPC icinden
        - protocol: TCP
          port: 9090

  egress:
    # CoreDNS
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kube-system
          podSelector:
            matchLabels:
              k8s-app: kube-dns
      ports:
        - protocol: UDP
          port: 53
        - protocol: TCP
          port: 53

    # RDS Postgres - sadece VPC icine, sadece 5432
    - to:
        - ipBlock:
            cidr: 10.20.0.0/16 # VPC CIDR (deploy/terraform var.vpc_cidr)
      ports:
        - protocol: TCP
          port: 5432
